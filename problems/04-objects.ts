// This problem covers object literals and their types.

// 1.  Define a type alias named 'Student'. A Student should be an object with the following properties:
//     - 'studentID' of type 'number'
//     - 'firstName' of type 'string'
//     - 'lastName' of type 'string'
//     - 'major' of type 'string'

// 2.  Create a constant named 'lguStudent' of type 'Student'.
//     Initialize it with an object literal that represents a student. You can make up the details,
//     but make sure the studentID is a non-zero number.

// Write your code below this line

export type Student = {
  // Define the properties here
};

export const lguStudent: Student = {
  // Initialize the object here
};